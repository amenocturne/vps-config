---
- name: Configure Claude Code UI server
  hosts: claudecodeui
  become: true
  vars_files:
    - "{{ playbook_dir }}/../../secrets.yml"
  roles:
    - { role: common, tags: ['common'] }
    - { role: security, tags: ['security'] }
    - { role: claudecodeui, tags: ['claudecodeui'] }
